<utils:BaseWindow
    x:Class="Celer.Views.Windows.AboutWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Celer.Views.Windows"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:utils="clr-namespace:Celer.Utilities"
    Title="About"
    ResizeMode="NoResize"
    ShowInTaskbar="False"
    SizeToContent="WidthAndHeight"
    WindowStartupLocation="CenterOwner"
    mc:Ignorable="d">
    <Border Padding="16">
        <StackPanel>
            <Image
                Width="156"
                Height="156"
                Source="/Resources/Assets/celer_256.png" />
            <TextBlock
                Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                Style="{StaticResource TitleLargeTextBlockStyle}"
                TextAlignment="Center">
                Celer
            </TextBlock>
            <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
                <TextBlock
                    Margin="0,0,4,0"
                    FontSize="14"
                    Foreground="{DynamicResource TextFillColorSecondaryBrush}">
                    Version:
                </TextBlock>
                <TextBlock
                    Name="Version"
                    FontSize="14"
                    Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                    TextAlignment="Center" />
            </StackPanel>
            <StackPanel Margin="0,16,0,0" HorizontalAlignment="Center">
                <TextBlock
                    Margin="0,0,0,4"
                    FontSize="14"
                    Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                    TextAlignment="Center">
                    Special thanks to:
                </TextBlock>
                <TextBlock FontSize="14" Foreground="{DynamicResource TextFillColorSecondaryBrush}">Flixin for feedback and testing</TextBlock>
            </StackPanel>
            <WrapPanel Margin="0,12,0,0">
                <WrapPanel.Resources>
                    <Style BasedOn="{StaticResource {x:Type TextBlock}}" TargetType="{x:Type TextBlock}">
                        <Setter Property="Margin" Value="0,0,16,0" />
                    </Style>
                </WrapPanel.Resources>
                <TextBlock><Hyperlink utils:HyperlinkExtensions.IsExternal="True" NavigateUri="https://surfscape.eu/">SurfScape Website</Hyperlink></TextBlock>
                <TextBlock><Hyperlink utils:HyperlinkExtensions.IsExternal="True" NavigateUri="https://surfscape.eu/celer">Celer Homepage</Hyperlink></TextBlock>
                <TextBlock Margin="0"><Hyperlink utils:HyperlinkExtensions.IsExternal="True" NavigateUri="https://surfscape.eu/support/">Support</Hyperlink></TextBlock>
            </WrapPanel>
            <TextBlock
                Margin="0,16,0,0"
                FontSize="13"
                Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                TextAlignment="Center">
                ©️ 2026 SurfScape and contributors
            </TextBlock>
        </StackPanel>
    </Border>
</utils:BaseWindow>
