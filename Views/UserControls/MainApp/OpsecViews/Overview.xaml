<UserControl x:Class="Celer.Views.UserControls.MainApp.OpsecViews.Overview"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:Celer.Converters"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:local="clr-namespace:Celer.Converters"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:Celer.ViewModels.OpsecVM"
             d:DataContext="{d:DesignInstance Type=vm:OverviewViewModel}"
             d:DesignHeight="450"
             d:DesignWidth="800"
             mc:Ignorable="d">
    <UserControl.Resources>
        <converters:BoolToBrushConverter x:Key="BoolToBrushConverter" />
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <UniformGrid Columns="2">
            <Border Margin="0,0,6,0"
                    Padding="12,8"
                    Background="{DynamicResource ControlFillColorDefaultBrush}"
                    BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="4">
                <StackPanel>
                    <TextBlock Style="{StaticResource Heading2}"
                               Text="Privacy" />
                    <WrapPanel>
                        <TextBlock Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                   Style="{DynamicResource TitleLargeTextBlockStyle}"
                                   Text="{Binding PrivacyScore}" />
                        <TextBlock Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                   Margin="6,0,0,5"
                                   VerticalAlignment="Bottom"
                                   Text="/ 10" />
                    </WrapPanel>
                </StackPanel>
            </Border>
            <Border Margin="6,0,0,0"
                    Padding="12,8"
                    Background="{DynamicResource ControlFillColorDefaultBrush}"
                    BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="4">
                <StackPanel>
                    <TextBlock Style="{StaticResource Heading2}"
                               Text="Security" />
                    <WrapPanel>
                        <TextBlock Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                   Style="{DynamicResource TitleLargeTextBlockStyle}"
                                   Text="{Binding SecurityScore}" />
                        <TextBlock Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                   Margin="6,0,0,5"
                                   VerticalAlignment="Bottom"
                                   Text="/ 10" />
                    </WrapPanel>
                </StackPanel>
            </Border>
        </UniformGrid>
        <StackPanel Orientation="Horizontal"
                    Grid.Row="1"
                    Margin="0,12,0,0">
            <StackPanel.Resources>
                <Style BasedOn="{StaticResource {x:Type Button}}"
                       TargetType="{x:Type Button}">
                    <Setter Property="Margin"
                            Value="0,0,16,0"></Setter>
                </Style>
            </StackPanel.Resources>
            <Button Command="{Binding RefreshCommand}">
                <StackPanel Orientation="Horizontal">
                    <iconPacks:PackIconLucide Margin="0,0,8,0"
                                               VerticalAlignment="Center"
                                               Kind="RefreshCw" />
                    <TextBlock Foreground="{DynamicResource TextFillColorPrimaryBrush }">Refresh</TextBlock>
                </StackPanel>
            </Button>
            <Button Command="{Binding OpenPrivacySettingsCommand}">
                <StackPanel Orientation="Horizontal">
                    <iconPacks:PackIconLucide Margin="0,0,8,0"
                                               VerticalAlignment="Center"
                                               Kind="EarthLock" />
                    <TextBlock Foreground="{DynamicResource TextFillColorPrimaryBrush }">Privacy Settings</TextBlock>
                </StackPanel>
            </Button>
            <Button Command="{Binding OpenDefenderCommand}">
                <StackPanel Orientation="Horizontal">
                    <iconPacks:PackIconLucide Margin="0,0,8,0"
                                               VerticalAlignment="Center"
                                               Kind="Shield" />
                    <TextBlock Foreground="{DynamicResource TextFillColorPrimaryBrush }">Windows Defender</TextBlock>
                </StackPanel>
            </Button>
        </StackPanel>
        <Grid Grid.Row="2"
              Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0"
                  Margin="0,0,10,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock Margin="0,0,0,10"
                           Style="{DynamicResource SubtitleTextBlockStyle}"
                           Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                           Text="Privacy Results" />
                <ItemsControl Grid.Row="1"
                              ItemsSource="{Binding PrivacyItems}"
                              FocusVisualStyle="{x:Null}"
                              ScrollViewer.VerticalScrollBarVisibility="Visible">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="auto"></ColumnDefinition>
                                    <ColumnDefinition Width="*"></ColumnDefinition>
                                </Grid.ColumnDefinitions>
                                <Ellipse Width="12"
                                         Height="12"
                                         Margin="0,0,10,0"
                                         Fill="{Binding IsEnabled, Converter={StaticResource BoolToBrushConverter}}" />
                                <Grid Grid.Column="1">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="auto"></RowDefinition>
                                        <RowDefinition Height="auto"></RowDefinition>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"></ColumnDefinition>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock FontSize="16"
                                               FontWeight="DemiBold"
                                               Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                               Text="{Binding Title}" />
                                    <TextBlock Text="{Binding Description}"
                                               Margin="0,2,0,0"
                                               Grid.Row="1"
                                               TextWrapping="Wrap"
                                               Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                                </Grid>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
            <Grid Grid.Column="1"
                  Margin="0,0,10,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0"
                           Margin="0,0,0,10"
                           Style="{DynamicResource SubtitleTextBlockStyle}"
                           Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                           Text="Security Results" />

                <ItemsControl Grid.Row="1"
                              FocusVisualStyle="{x:Null}"
                              ItemsSource="{Binding SecurityItems}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="auto"></ColumnDefinition>
                                    <ColumnDefinition Width="*"></ColumnDefinition>
                                </Grid.ColumnDefinitions>
                                <Ellipse Width="12"
                                         Height="12"
                                         Margin="0,0,10,0"
                                         Fill="{Binding IsEnabled, Converter={StaticResource BoolToBrushConverter}}" />
                                <Grid Grid.Column="1">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="auto"></RowDefinition>
                                        <RowDefinition Height="auto"></RowDefinition>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"></ColumnDefinition>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock FontSize="16"
                                               FontWeight="DemiBold"
                                               Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                               Text="{Binding Title}" />
                                    <TextBlock Text="{Binding Description}"
                                               Margin="0,2,0,0"
                                               Grid.Row="1"
                                               TextWrapping="Wrap"
                                               Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                                </Grid>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
