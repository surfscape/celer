<UserControl
    x:Class="Celer.Views.UserControls.MainApp.Manutenção"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
    xmlns:local="clr-namespace:Celer.Views.UserControls.MainApp"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="clr-namespace:Celer.ViewModels"
    d:DataContext="{d:DesignInstance Type=viewmodels:ManutencaoViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Border Padding="12,6">
        <StackPanel>
            <ContentControl Grid.Row="1" Content="{Binding CurrentView}">
                <ContentControl.Style>
                    <Style TargetType="ContentControl">
                        <Setter Property="Visibility" Value="Visible" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding CurrentView}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ContentControl.Style>
            </ContentControl>


            <Grid>
                <Grid.Style>
                    <Style TargetType="Grid">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding CurrentView}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Border
                    Margin="0,0,0,6"
                    Padding="0,0,0,6"
                    BorderBrush="Gray"
                    BorderThickness="0,0,0,1">
                    <StackPanel>
                        <TextBlock Style="{StaticResource Heading1}">Manutenção &amp; Recuperação</TextBlock>
                        <TextBlock Style="{StaticResource Subheading}">Ferramentas para realizar manutenção do sistema e também a sua recuperação</TextBlock>
                    </StackPanel>
                </Border>
                <StackPanel Grid.Row="1">


                    <UniformGrid Margin="-10,-5,0,0">
                        <UniformGrid.Resources>
                            <Style BasedOn="{StaticResource {x:Type Button}}" TargetType="Button">
                                <Setter Property="Margin" Value="10,10,0,0" />
                                <Setter Property="Padding" Value="12" />
                                <Setter Property="HorizontalContentAlignment" Value="Left" />
                                <Setter Property="VerticalContentAlignment" Value="Top" />
                            </Style>
                        </UniformGrid.Resources>
                        <Button Command="{Binding NavigateCommand}" CommandParameter="BatteryManagement">
                            <StackPanel>
                                <StackPanel Margin="0,0,0,4" Orientation="Horizontal">
                                    <iconPacks:PackIconLucide
                                        Width="20"
                                        Height="20"
                                        Margin="0,0,8,0"
                                        VerticalAlignment="Center"
                                        Foreground="{DynamicResource SecondaryText}"
                                        Kind="Battery" />
                                    <TextBlock FontSize="18" Foreground="White">Gestor de Bateria</TextBlock>
                                </StackPanel>
                                <TextBlock TextWrapping="Wrap">Verfique o estado da bateria do computador (amperagem, saúde, e avisos)</TextBlock>
                            </StackPanel>
                        </Button>
                        <Button>
                            <StackPanel>
                                <StackPanel Margin="0,0,0,4" Orientation="Horizontal">
                                    <iconPacks:PackIconLucide
                                        Width="20"
                                        Height="20"
                                        Margin="0,0,8,0"
                                        VerticalAlignment="Center"
                                        Foreground="{DynamicResource SecondaryText}"
                                        Kind="Wrench" />
                                    <TextBlock FontSize="18" Foreground="white">Diagnóstico e Reparação do Sistema</TextBlock>
                                </StackPanel>
                                <TextBlock TextWrapping="Wrap">Realiza diferentes checks para a reparação de diversos componentes do sistema operativo</TextBlock>
                            </StackPanel>
                        </Button>
                        <Button>
                            <StackPanel>
                                <StackPanel Margin="0,0,0,4" Orientation="Horizontal">
                                    <iconPacks:PackIconLucide
                                        Width="20"
                                        Height="20"
                                        Margin="0,0,8,0"
                                        VerticalAlignment="Center"
                                        Foreground="{DynamicResource SecondaryText}"
                                        Kind="MonitorCheck" />
                                    <TextBlock FontSize="18" Foreground="white">Gestor Gráfico</TextBlock>
                                </StackPanel>
                                <TextBlock TextWrapping="Wrap">Configurações do adaptador gráfico e do gestor de janelas do Windows</TextBlock>
                            </StackPanel>
                        </Button>
                    </UniformGrid>
                </StackPanel>

            </Grid>
        </StackPanel>
    </Border>
</UserControl>
