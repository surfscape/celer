<UserControl x:Class="Celer.Views.Components.ModuleHeader"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Celer.Views.Components"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:converters="clr-namespace:Celer.Converters"
             x:Name="Root"
             mc:Ignorable="d"
             d:DesignHeight="600"
             d:DesignWidth="800">
    <UserControl.Resources>
        <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter" />
    </UserControl.Resources>
    <Grid Height="610" Visibility="{Binding CurrentView, Converter={StaticResource NullToVisibilityConverter}, ElementName=Root}">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Border Padding="16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"></RowDefinition>
                    <RowDefinition Height="auto"></RowDefinition>
                </Grid.RowDefinitions>
                <TextBlock Style="{StaticResource Heading1}"
                           Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                           Text="{Binding ViewLabel, ElementName=Root}" />
                <TextBlock FontSize="15"
                           Grid.Row="1"
                           Margin="0,-4,0,0"
                           VerticalAlignment="Center"
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                           Text="{Binding ViewDescription, ElementName=Root}" />
            </Grid>
        </Border>
        <Border Padding="16,8,16,16" Grid.Row="1">
            <ContentControl Content="{Binding CurrentView, ElementName=Root}" />
        </Border>
    </Grid>
</UserControl>
