<UserControl x:Class="Celer.Views.Components.ModuleButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Celer.Views.Components"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d"
             x:Name="Root"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <Button Command="{Binding NavigateCommand, ElementName=Root}"
            CommandParameter="{Binding NavigateTo, ElementName=Root}"            
            >
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <iconPacks:PackIconLucide Width="24"
                                      Height="24"
                                      Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                      Kind="{Binding Icon, ElementName=Root}" />
            <StackPanel Grid.Column="1" Margin="8,0,0,0">
                <TextBlock FontSize="15"
                    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                           Text="{Binding Label, ElementName=Root}" Margin="0,0,0,4"></TextBlock>
                <TextBlock TextWrapping="Wrap"
                           Style="{DynamicResource BodyTextBlockStyle}"
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                           Text="{Binding Description, ElementName=Root}"></TextBlock>
            </StackPanel>
        </Grid>
    </Button>
</UserControl>
